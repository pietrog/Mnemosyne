<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    style="@style/wordobjectname"
    >
    <TextView
        android:text="TEXT"
        android:id="@+id/word_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/description"
        android:layout_alignParentTop="true"
        />

    <TextView
        android:id="@+id/word_definition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/description"
        android:layout_below="@+id/word_name"/>

    <TextView
        android:id="@+id/hintNextDate"
        android:text="@string/hin t_last_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/word_definition" />
    <TextView
        android:id="@+id/lastDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/hintNextDate" />

    <TextView
        android:id="@+id/hintLastDate"
        android:text="@string/hint_next_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/lastDate" />
    <TextView
        android:id="@+id/nextDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/hintLastDate"
        />

</RelativeLayout>